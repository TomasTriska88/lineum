{
  "$schema": "https://railway.app/v2.schema.json",
  "services": {
    "portal": {
      "source": {
        "rootDirectory": "./"
      },
      "build": {
        "buildCommand": "mkdir -p portal/src/lib/data/whitepapers && mkdir -p portal/src/lib/data/source && cp -r whitepapers/* portal/src/lib/data/whitepapers/ && cp -r source/* portal/src/lib/data/source/ && cd portal && npm install && npm run build",
        "watchPatterns": [
          "portal/**",
          "whitepapers/**",
          "source/**"
        ]
      },
      "deploy": {
        "startCommand": "cd portal && node build",
        "healthCheckPath": "/wiki/lineum-core",
        "restartPolicyType": "ON_FAILURE"
      }
    },
    "lab": {
      "source": {
        "rootDirectory": "lab"
      }
    }
  }
}