{
  "$schema": "https://railway.app/v2.schema.json",
  "services": {
    "portal": {
      "source": {
        "rootDirectory": "./"
      },
      "build": {
        "buildCommand": "echo '--- DIAGNOSTIC START ---' && ls -la && ls -la portal && mkdir -p portal/src/lib/data/whitepapers && mkdir -p portal/src/lib/data/source && cp -rv whitepapers/* portal/src/lib/data/whitepapers/ && cp -rv source/* portal/src/lib/data/source/ && echo '--- SYNC VERIFICATION ---' && ls -R portal/src/lib/data && cd portal && npm install && npm run build",
        "watchPatterns": [
          "portal/**",
          "whitepapers/**",
          "source/**"
        ]
      },
      "deploy": {
        "startCommand": "cd portal && node build",
        "healthCheckPath": "/wiki/lineum-core",
        "restartPolicyType": "ON_FAILURE"
      }
    },
    "lab": {
      "source": {
        "rootDirectory": "lab"
      }
    }
  }
}